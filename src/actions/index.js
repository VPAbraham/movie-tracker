
export const saveMovies = (movies) => ({
  type: "SAVE_MOVIES",
  movies
})

export const saveUser = (user) => ({
  type: "SAVE_USER",
  user
})

export const saveFavorites = (favorites) => ({
  type: "SAVE_FAVORITES"
})